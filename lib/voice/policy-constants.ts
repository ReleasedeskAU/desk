/**
 * Shared Voice policy constants safe for client + server bundles.
 * Keep Prisma / usage imports out of this file.
 */

/** Default daily connected minutes for every user (admin can raise or set unlimited). */
export const VOICE_DEFAULT_DAILY_MINUTES = 10;
