# Schema moved

The canonical Prisma schema now lives at:

`packages/database/prisma/schema.prisma` (`@releasedesk/database`)

A pre-extraction backup is kept as `schema.prisma.bak`.
Do not recreate a local `schema.prisma` here.
