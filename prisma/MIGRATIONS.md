# Migrations moved

Canonical migration history now lives at:

`packages/database/prisma/migrations`

The copy under `Sentinel/prisma/migrations` is a transitional backup.
New migrations must be created against `@releasedesk/database` only.
